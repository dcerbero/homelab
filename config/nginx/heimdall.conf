
server {
    listen 80; # Nginx escuchará las peticiones HTTP en el puerto 80
    listen [::]:80;
    server_name heimdall.local; # Define el nombre de host para este proxy. Cámbialo si usas otro.

    # Configuración de proxy para reenviar tráfico a Jellyfin (contenedor Docker)
    location /services/heimdall/ {
        proxy_pass http://svcHeimdall:80; # 'jellyfin' es el nombre del servicio Docker, 8096 es su puerto interno
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}