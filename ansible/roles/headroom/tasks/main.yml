---
- name: Create headroom data directory
  ansible.builtin.file:
    path: "{{ PATH_DATA }}/ia/headroom"
    state: directory
    owner: "1000"
    group: "1000"
    mode: '0755'
    recurse: yes
  become: true
