---
- name: Setup homeserver
  hosts: homeserver
  become: true
  
  roles:
    - system-setup
    - docker
    - pihole
    - tailscale
    - cadvisor
    - openclaw
    - nginx

  post_tasks:
    - name: homeserver setup completed
      debug:
        msg: |
          ✅ homeserver setup completed successfully!