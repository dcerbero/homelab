---
- name: Setup homeserver
  hosts: homeserver
  become: true
  gather_facts: no

  pre_tasks:
    - name: Gather facts as connection user
      ansible.builtin.setup:
      become: false
      tags: always

  roles:
    - role: system-setup
      tags: [system-setup, system]
    - role: docker
      tags: [docker, containers]
    - role: pihole
      tags: [pihole, dns]
    - role: tailscale
      tags: [tailscale, vpn]
    - role: cadvisor
      tags: [cadvisor, monitoring]
    - role: openclaw
      tags: [openclaw, ia]

    - role: heimdall
      tags: [heimdall, dashboard]
    - role: nginx
      tags: [nginx, proxy]

- name: Setup Oracle Cloud VM (Pi-hole failover)
  hosts: oracle
  become: true
  gather_facts: no

  pre_tasks:
    - name: Gather facts as connection user
      ansible.builtin.setup:
      become: false
      tags: always

  roles:
    - role: system-setup
      tags: [system-setup, system, oracle]
    - role: docker
      tags: [docker, containers, oracle]
    - role: pihole
      tags: [pihole, dns, oracle]
