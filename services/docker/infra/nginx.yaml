services:
  svcNginx:
    image: nginx:1.30.3-bookworm
    container_name: nginx_proxy
    profiles:
      - infra
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "${PATH_DATA}/homelab/config/nginx/conf.d:/etc/nginx/conf.d:ro"
